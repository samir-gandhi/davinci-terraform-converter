There are a number of bugs in the current build. Use this prompt to create a phased approach and test driven development to work through this bug. 

## Bug 10

The project intends to run `make devcheck` before committing code changes to ensure code quality and consistency. However, currently `make devcheck` is failing in a number of areas. Update the codebase to resolve all issues causing `make devcheck` to fail, ensuring that it passes successfully without any errors. Add any necessary tests to verify that the code quality checks are functioning as expected. If functions are unused and not needed, remove them to maintain a clean codebase.